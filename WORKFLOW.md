What did the rejected push error message tell you, and why did it happen?

&#x09;- it said ! \[rejected] main -> main (fetch first), failed to push some refs to the GitHub link. it also said that updates were rejected because the remote contains work that I do not have locally so I may want to first integrate the remote changes by pulling it.



What's the actual difference between how you resolved Task 3 (merge) vs Task 4 (rebase)?

&#x09;- for the both they're actually pretty similar. It said that I need to fetch origin first, so I did. then i merge/rebase but for rebase, I had to remove lines of codes "<<<<< ===== >>>>" before I was able to rebase -- continue instead. 



What one habit would have avoided both rejected pushes in this lab?

&#x09;- The only habit that would have avoided these problems was to ALWAYS PULL first before working on a project, it may not really work if both are simultaneously working, but its a great safety measure just to make sure that our local repo has the copies from the remote one.



Which approach - merge or rebase - would you default to on a shared team branch, and why?

&#x09;- I would rather merge because its much safer and not mess up with other members of the team.

